#include <stdio.h>
#include <stdlib.h>
struct node{
    int data;
    struct node* next;
};
typedef struct node Node;
int main(){
    int n,i,data;
    printf("Enter the no of  elements:");
    
    for(i=0;i<n;i++){
        
    }
}